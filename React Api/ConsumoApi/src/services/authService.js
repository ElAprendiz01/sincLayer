// authService.js
// Aquí van tus funciones de autenticación (fetch / axios / etc.)

export async function login(email, password) {
  // Ejemplo básico, reemplaza por tu API real:
  return { success: true, token: 'token-demo' };
}
